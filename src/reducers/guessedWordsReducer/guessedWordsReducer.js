
const initialState = [];

/**
 *
 * @param {arrary} state - Array of guessed words.
 * @param {object} action - actions to be reducer
 * @returns {arrary} - new state
 */
export const guessedWordsReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};